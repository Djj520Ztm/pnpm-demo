{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "graph": "nx graph",
    "dev": "lerna run dev",
    "test": "lerna run test",
    "build": "lerna run build",
    "build:header": "lerna run build --scope=header",
    "build:footer": "lerna run build --scope=footer",
    "build:remixapp": "lerna run build --scope=remixapp",
    "pub": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
